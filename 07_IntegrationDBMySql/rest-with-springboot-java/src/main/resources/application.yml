spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_with_spring_boot_erudio
    username: root
    password: Senha!123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # ou validate, create, create-drop, conforme necessário
    show-sql: true # Para exibir as consultas SQL no console
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  jackson:
    serialization:
      indent-output: true # Formatar a saída JSON (opcional)
